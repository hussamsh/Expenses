<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infinitetech.expenses" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="com.infinitetech.expenses.ActivityClasses.InsertActivity"
            android:label="MainActivity" >
        </activity>

        <receiver android:name="com.infinitetech.expenses.ExpensesPdfFactory" />

        <activity
            android:name="com.infinitetech.expenses.ActivityClasses.SendActivity"
            android:label="@string/title_activity_send"
            android:parentActivityName="com.infinitetech.expenses.ActivityClasses.InsertActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.infinitetech.expenses.ActivityClasses.InsertActivity" />
        </activity>
        <activity
            android:name=".ActivityClasses.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
